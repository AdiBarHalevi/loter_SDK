const BASE_URL = 'https://the-one-api.dev/v2/'
const SEARCH_PARAMS_ENUM = {
  id: '_id',
  name: 'name',
  order: 'order'
}

const NUMBER_OF_BOOKS = 3

module.exports = {BASE_URL, SEARCH_PARAMS_ENUM,NUMBER_OF_BOOKS}